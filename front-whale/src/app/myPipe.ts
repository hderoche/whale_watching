import { Pipe, PipeTransform } from '@angular/core';
/*
@Pipe({name: 'color'})
export class Color implements PipeTransform {
  transform(value: string, ): string {
    let newStr = `${value} add smth`;
    return newStr;
  }
}
*/